{
  "name": "@skarion/identity",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo \"(stub) no build yet for @skarion/identity\"",
    "dev": "wrangler dev --port 8787 --inspector-port 9230",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx ../../packages/db-kit/scripts/migrate-cli.ts --folder=./drizzle"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.1",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "@skarion/auth-client": "workspace:^",
    "@skarion/db-kit": "workspace:*",
    "@skarion/ui": "workspace:^",
    "@types/bcryptjs": "^3.0.0",
    "bcryptjs": "^3.0.3",
    "drizzle-orm": "0.45.2",
    "hash-wasm": "^4.12.0",
    "hono": "^4.12.26",
    "kysely": "0.29.2"
  }
}
