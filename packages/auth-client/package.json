{
  "name": "@skarion/auth-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo \"(stub) no build yet for @skarion/auth-client\"",
    "dev": "echo \"(stub) no dev yet for @skarion/auth-client\"",
    "test": "echo \"(stub) no tests yet for @skarion/auth-client\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "hono": "^4.12.26"
  }
}
