{
  "name": "@skarion/db-kit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo \"(stub) no build yet for @skarion/db-kit\"",
    "dev": "echo \"(stub) no dev yet for @skarion/db-kit\"",
    "test": "echo \"(stub) no tests yet for @skarion/db-kit\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "drizzle-orm": "^0.45.2"
  }
}
